int main(int argc, char const **argv)
{
    //Process command line arguments

    //Initialize MPI with multiple thread support

    //Construct Server object for master process
    //Start thread to process Server for master process

    //Constract Client object and start processing

    //Wait for Server thread

    //Shutdown MPI
	return 0;
}
